<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="cameva_payroll.list">
      <field name="name">cameva_payroll list</field>
      <field name="model">cameva_payroll.cameva_payroll</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="cameva_payroll.action_window">
      <field name="name">cameva_payroll window</field>
      <field name="res_model">cameva_payroll.cameva_payroll</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="cameva_payroll.action_server">
      <field name="name">cameva_payroll server</field>
      <field name="model_id" ref="model_cameva_payroll_cameva_payroll"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="cameva_payroll" id="cameva_payroll.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="cameva_payroll.menu_1" parent="cameva_payroll.menu_root"/>
    <menuitem name="Menu 2" id="cameva_payroll.menu_2" parent="cameva_payroll.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="cameva_payroll.menu_1_list" parent="cameva_payroll.menu_1"
              action="cameva_payroll.action_window"/>
    <menuitem name="Server to list" id="cameva_payroll" parent="cameva_payroll.menu_2"
              action="cameva_payroll.action_server"/>
    -->


    <!-- 

      attrs="{'invisible':[('amount_select','=','code')], 'required':[('amount_select','!=','code')]}"
     -->
  
    <record model="ir.ui.view" id="cameva_payroll.quantity">
      <field name="name">Enabling quantity for python code</field>
      <field name="model">hr.salary.rule</field>
      <field name="inherit_id" ref="hr_payroll.hr_salary_rule_form"/>
      <field name="arch" type="xml">
        <xpath expr="//field[@name='quantity']" position="replace">
          <field 
          name="quantity" 
          attrs="{'invisible':[], 'required':[('amount_select','!=','')]}"
          />
        </xpath>
      </field>
    </record>
  
  </data>
</odoo>